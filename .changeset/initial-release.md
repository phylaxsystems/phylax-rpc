---
"@phylaxsystems/phylax-rpc": minor
---

Initial public release. Headless Phylax RPC switch library: EIP-6963 wallet detection, credible-require preflight, and assisted EIP add/switch/verify, with `react`, `wagmi`, `viem`, and `ethers` adapters plus a manual-add modal. Published as ESM-only, Node 22+.
