---
"@phylax-systems/phylax-rpc": patch
---

Correct the npm scope to `@phylax-systems` across the package name, import paths, and documentation. The npm org uses a hyphen (`@phylax-systems`) even though the GitHub org does not (`phylaxsystems`).
